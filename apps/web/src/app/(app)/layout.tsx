// Authenticated app layout — dashboard, sermon review, settings.
// Future home for app shell (sidebar, top nav, user menu).
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
